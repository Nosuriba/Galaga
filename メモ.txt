[やること]

・ｱﾆﾒｰｼｮﾝ付きの描画(std::map) dataの部分を配列の何番目かを指定する
  [RectGraphで描画しているので、その部分を考える]

・[画像ID(描画する画像の位置), フレーム数] コマ数分ほしい　→ vectorで実装するようにする
・
・
・


描画位置の計算 → Vector2((ID % 横のｺﾏの最大値) * size, (ID / 横のｺﾏの最大値) * size )

※ﾊﾟｯﾄﾞ入力でも実装できたらいいな(ｷｰｺﾝﾌｨｸﾞがまだ不完全なのだよ...)

入力の流れ


書くこと
・debug用のｸﾗｽにAST()を書いておく, InputStateにも	(アサートは後でやる)
・スロウ機能
  一回当たりの移動するフレームと移動量を可変になる
　chrono タイムポイントを使う(using) start と　endを使う　waittimeはdouble
  ＋記と※記号を同時におすと掛け算
　waittimeよりstartとendの差が低かったとき処理を抜ける
　(処理を途中で終わりたい時があるので、ProcessMessage() != 1 || CheckHitKey(KEY_INPUT_ESCAPE)
　で抜けるようにする)
　chronoの機能でnowがあるので、endとstartの差分を求めてから使うように
・
・

※constで書いている変数を初期化