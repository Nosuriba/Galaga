[やること]

・ｱﾆﾒｰｼｮﾝ付きの描画(std::map) dataの部分を配列の何番目かを指定する
  [RectGraphで描画しているので、その部分を考える]

・[画像ID(描画する画像の位置), フレーム数] コマ数分ほしい　→ vectorで実装するようにする
・
・
・


描画位置の計算 → Vector2((ID % 横のｺﾏの最大値) * size, (ID / 横のｺﾏの最大値) * size )

※ﾊﾟｯﾄﾞ入力でも実装できたらいいな(ｷｰｺﾝﾌｨｸﾞがまだ不完全なのだよ...)

入力の流れ


書くこと
・debug用のｸﾗｽにAST()を書いておく, InputStateにも	(アサートは後でやる)
・描画周りを作っていく
・SceneMng(Game)にDraw関数を作る

・DebugConOutのｸﾗｽを参考にDebugDispOutというｸﾗｽを作って、Debug情報を描画するように実装してみる
・DebugDispOutをシングルトンにする
・DrawGraph, DrawLine, DrawBox, DrawCircleのラッパー関数を用意する
・_dbgの後ろにDrawGraphなどの関数をつけるようにする


・ｽｸﾘｰﾝに対して描画を行うようにする
・Queに対しての登録はtupleというものを使う

※SceneのUpdateを返り値uniqur_ptrで返してループするような仕組みを実装しておく
※constで書いている変数を初期化