[やること]

・ｱﾆﾒｰｼｮﾝ付きの描画(std::map) dataの部分を配列の何番目かを指定する
  [RectGraphで描画しているので、その部分を考える]

・[画像ID(描画する画像の位置), フレーム数] コマ数分ほしい　→ vectorで実装するようにする
・
・
・


描画位置の計算 → Vector2((ID % 横のｺﾏの最大値) * size, (ID / 横のｺﾏの最大値) * size )

※ﾊﾟｯﾄﾞ入力でも実装できたらいいな(ｷｰｺﾝﾌｨｸﾞがまだ不完全なのだよ...)

入力の流れ

モジュールテストを行って、動作中におかしな値が入った時、どういう処理になるのかの確認をなるべく
行うようにする
(Break Pointを活用してみようか)


書くこと
・debug用のｸﾗｽにAST()を書いておく, InputStateにも	(アサートは後でやる)
※constで書いている変数を初期化

・三種類の敵がインスタンスできるようにする				〇
・敵を特定の位置からインスタンスできるようにする			〇
・敵を目標の地点に移動できるようにする					〇
・目標地点までの移動ができたら、次は中間点までの移動を行って		〇

・ｱﾆﾒｰｼｮﾝを最初に登録した敵と同じ動きをするようにしておく		△

	
・敵の回転をさせておく
・単体→編隊の配置にできるようにする(ｱﾆﾒｰｼｮﾝは戦闘と同じ動きにする)
・戦闘が目標地点に向かおうとしたとき、後ろの敵も目標地点に向かうようにする
・シグモイド関数を使って、敵の登場部分の移動を実装する
・最初に登場した敵の情報を[shared]もしくは[weak]の情報を持つようにする
・敵の位置をテーブル化しておくようにする	

※シグモイド関数の参考資料
http://www.c-tipsref.com/tips/math/sigmoid.html

新しいこと
#include <algorithm>
std::remove_if(開始位置, 終了位置, プレディケート(真偽を返す関数を渡す))
st::remove(開始位置, 終了位置, 比べるもの)